# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file with actual credentials to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Azure OpenAI Configuration
# -----------------------------------------------------------------------------
# Your Azure OpenAI endpoint URL
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/

# Your Azure OpenAI API key
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here

# Azure OpenAI deployment name for chat model (default: gpt-4o)
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME=gpt-4o

# Azure OpenAI deployment name (default: gpt-4o)
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# -----------------------------------------------------------------------------
# AWS Configuration
# -----------------------------------------------------------------------------
# AWS Access Key ID for S3 and other AWS services
AWS_ACCESS_KEY_ID=your-aws-access-key-id

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# AWS Region (default: us-east-1)
AWS_REGION=us-east-1

# S3 Bucket name for storing scan results
S3_BUCKET=your-s3-bucket-name

# -----------------------------------------------------------------------------
# Scan Configuration
# -----------------------------------------------------------------------------
# Path to the folder to scan (for Docker: /scan)
SCAN_FOLDER_PATH=/scan

# -----------------------------------------------------------------------------
# MITRE MCP Server Configuration
# -----------------------------------------------------------------------------
# URL to the MITRE MCP server (Docker internal: http://mitre-mcp:8000/mcp)
MITRE_MCP_URL=http://mitre-mcp:8000/mcp
