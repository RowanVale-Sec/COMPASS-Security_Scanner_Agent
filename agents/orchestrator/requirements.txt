# COMPASS Orchestrator Agent Dependencies

# Microsoft Agent Framework
agent-framework==1.0.0b251016
agent-framework-core==1.0.0b251016
agent-framework-azure-ai==1.0.0b251016
azure-ai-agents==1.2.0b5
azure-ai-projects==1.1.0b4

# OpenAI and Azure Integration (used when LLM_PROVIDER=azure)
openai>=1.109.0
azure-identity>=1.25.0
azure-core>=1.36.0

# Anthropic Claude (used when LLM_PROVIDER=claude)
anthropic>=0.40.0

# JSON Schema validation for structured LLM outputs
jsonschema>=4.0

# Data Processing and Validation
pydantic>=2.12.0
requests>=2.32.0
python-dotenv>=1.1.0

# Web Framework (HTTP API)
flask>=3.0.0

# Cloud Run service-to-service auth (no-op locally)
google-auth>=2.30.0
